// Re-export for backward compatibility — pages that imported Navbar directly
// This is a no-op placeholder; global navigation is now handled by AppSidebar + GlobalBar
"use client";

export function Navbar() {
  return null;
}
